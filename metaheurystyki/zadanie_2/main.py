import random

from bitarray.util import urandom

from genetic_algorighm import genetic_algorithm, calculate_adaptation, random_individual, cross_genes, make_pairs

# main function
# todo
if __name__ == '__main__':
    # genetic_algorithm(50, 10)
    # first = urandom(26)
    # second = urandom(26)
    # pair = [first, second]
    # print(first)
    # print(second)
    # pivot = random.randint(1, 25)
    # print(pivot)
    # cross_genes(pair, pivot)

    debug = [1,4,5,6,7]
    print(make_pairs(debug))





